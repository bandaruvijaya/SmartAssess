# Application Settings

# Database: SQLite (auto-created, no configuration needed)
# Database file: smartassess.db (created automatically on startup)

# API Keys (Optional - for advanced features)
GEMINI_API_KEY=your_gemini_api_key_here

# Server Configuration
PORT=5000
APP_ENV=production

# Demo mode (bypass auth to access recommendations)
DEMO_MODE=1

# Optional: Embedding Model Path (if using local model)
# EMBED_MODEL_PATH=/path/to/local/model
